# Calculator in C++

A simple calculator program written in C++ using a `switch` statement.  
This program performs basic arithmetic operations:

- Addition (`+`)
- Subtraction (`-`)
- Multiplication (`*`)
- Division (`/`)
